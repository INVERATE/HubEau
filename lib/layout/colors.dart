import 'package:flutter/material.dart';

class BluePalette {
  // Couleur principale (utilisée pour les boutons, AppBar, liens actifs)
  static const Color primary = Colors.indigoAccent; // Bleu indigo
  // Couleur secondaire (survols, hover, éléments actifs)
  static const Color secondary = Color(0xFF42A5F5); // Bleu clair
  // Couleur d'accent (pour mettre en avant un élément important)
  static const Color accent = Color(0xFF82B1FF); // Bleu ciel pastel
  // Fond principal de l'application
  static const Color background = Color(0xFFF0F4FF); // Bleu très pâle
  // Couleur des cartes ou sections secondaires
  static const Color surface = Color(0xFFE3F2FD); // Bleu/gris doux
  // Texte principal (foncé sur fond clair)
  static const Color textPrimary = Color(0xFF0D47A1); // Bleu profond
  // Texte secondaire (surfaces plus pâles ou désactivées)
  static const Color textSecondary = Color(0xFF546E7A); // Gris bleuté
  // Couleur des bordures ou lignes discrètes
  static const Color divider = Color(0xFFB0BEC5); // Gris clair
  // Erreur
  static const Color error = Color(0xFFD32F2F); // Rouge standard
}
